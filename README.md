# API Rest com Node JS, Express e MondoDB

### Atenção!
Para o funcionamento da aplicação é necessário que seja criado um diretório na raiz do projeto chamado "config" e, dentro dele, um arquivo JSON chamado config.json conforme abaixo:

- config
-- config.json